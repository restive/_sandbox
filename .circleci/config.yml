version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8
    steps:
      - checkout
      - run: echo "start"
      - run:
          name: 'install-dependencies'
          command: 'npm install'
      - run:
          name: 'run-tests'
          command: 'npm test'